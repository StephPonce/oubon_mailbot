# Ospra Intelligence - Python Dependencies
# Generated for Render deployment

# Web Framework
fastapi>=0.109.0,<0.119  # <0.119: starlette 1.x breaks conftest under filterwarnings=error
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
psycopg2-binary>=2.9.9

# HTTP/Async
aiohttp>=3.9.1
httpx>=0.26.0
requests>=2.31.0

# Email
resend>=0.7.0

# AI APIs
anthropic>=0.18.0
openai>=1.66.0  # >=1.66 required for the Responses API (xai_twitter Agent Tools)
google-generativeai>=0.3.2

# Google APIs (Gmail, etc)
google-api-python-client>=2.114.0
# Google Ads (ospra_os/advertising/ — 9 import sites). NOT pulled in by any
# other dep: it was pyproject-only, so ad deployment has been
# ImportError-disabled in every Render build. Third bite of the T176
# manifest drift (after cloudinary and posthog/otel).
google-ads>=25.0.0
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Authentication
python-jose[cryptography]>=3.3.0
# Fernet credential encryption — 27 import sites. Was only present as a
# transitive dep of python-jose[cryptography]; too load-bearing to leave
# implicit. Keep in sync with pyproject.toml. (T176 dependency-manifest
# reconciliation pass.)
cryptography>=41.0.0
# bcrypt is called directly from ospra_os/auth/jwt_auth.py — passlib was
# removed to avoid its broken self-test on bcrypt 4+ (see the comment in
# that module). Keep this pin in sync with pyproject.toml so a uv-based
# install and a pip-based install produce identical hashing behaviour.
bcrypt>=4.1.2

# Scheduling
apscheduler>=3.10.4

# Data Processing
pandas>=2.1.4
numpy>=1.26.3

# Utilities
python-dotenv>=1.0.0
# Timezone quiet-hours (advertising scheduler). Transitive via pandas until
# now — pinned explicitly so a pandas upgrade can't silently drop it. (T176)
pytz>=2024.1
tenacity>=8.2.3
cachetools>=5.3.2

# Image Processing
Pillow>=10.2.0

# Validation
email-validator>=2.1.0

# Redis (for caching/queues)
redis>=5.0.1

# Background task queue — imported by ospra_os/celery_app.py and ospra_os/tasks/*,
# run as worker/beat/flower in docker-compose.yml. Was previously UNDECLARED, so
# any environment installing from this file lacked Celery entirely and the task
# system silently degraded. Broker/result backend is the Redis dependency above.
celery>=5.3.0

# Analytics + LLM observability (T-PostHog). Render builds from THIS file,
# not pyproject.toml — these were pyproject-only, so PostHog analytics and
# OTel LLM tracing were silently disabled in prod (main.py catches the
# ImportError and logs a warning). Keep in sync with pyproject.toml.
posthog[otel]
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-anthropic>=0.40.0
opentelemetry-instrumentation-openai-v2>=2.0.0b0

# Cloud image storage (T160). Without this, ImageStorage logs "Cloudinary
# not installed" and silently falls back to ephemeral local disk even with
# creds set — product images would die on the next deploy.
cloudinary>=1.40.0

# T176 drift, second sweep (full pyproject-vs-requirements set-diff found 14
# undeclared packages; these 4 back live code):
# - facebook-business: hard import in advertising/meta — without it the entire
#   advertising router fails to load in prod.
# - sentry-sdk: observability/error_tracking — error tracking was silently
#   disabled in every Render build.
# - Amazon SP-API + PA-API: platforms/amazon.py and the Amazon discovery
#   connector degrade to disabled without them.
# Deliberately NOT added: praw (deprecated), rembg/onnxruntime (opt-in image
# feature, ~200MB build cost — needs a product decision), playwright /
# apify-client / fake-useragent / jinja2 / slack-sdk / aiosqlite / schedule
# (zero live imports or dev-only).
facebook-business>=24.0.0
sentry-sdk[fastapi,httpx,sqlalchemy]>=2.47.0
python-amazon-sp-api>=1.9.50
python-amazon-paapi==5.0.1

# Rate Limiting
slowapi>=0.1.9

# Pytrends
pytrends>=4.9.2

# System Monitoring
psutil>=5.9.0

# PDF Reports
reportlab>=4.0.0

# Web Scraping
beautifulsoup4>=4.12.0

# Microsoft OAuth (Email Sync)
msal>=1.24.0

# Testing
pytest>=7.4.0
pytest-asyncio>=1.3.0,<2  # match pyproject; 0.2x/1.x differ on event-loop teardown
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
httpx>=0.26.0
fakeredis>=2.20.0
