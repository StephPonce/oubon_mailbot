<section class="trust-badges scroll-reveal">
  <div class="container">
    <div class="trust-grid">
      <div class="trust-badge">
        <div class="trust-icon">🚚</div>
        <div class="trust-title">{{ section.settings.badge_1_title }}</div>
        <div class="trust-text">{{ section.settings.badge_1_text }}</div>
      </div>

      <div class="trust-badge">
        <div class="trust-icon">↩️</div>
        <div class="trust-title">{{ section.settings.badge_2_title }}</div>
        <div class="trust-text">{{ section.settings.badge_2_text }}</div>
      </div>

      <div class="trust-badge">
        <div class="trust-icon">💬</div>
        <div class="trust-title">{{ section.settings.badge_3_title }}</div>
        <div class="trust-text">{{ section.settings.badge_3_text }}</div>
      </div>

      <div class="trust-badge">
        <div class="trust-icon">🔒</div>
        <div class="trust-title">{{ section.settings.badge_4_title }}</div>
        <div class="trust-text">{{ section.settings.badge_4_text }}</div>
      </div>
    </div>
  </div>
</section>

<style>
.trust-badges {
  padding: 3rem 0;
  background: var(--color-gray-50);
}

.trust-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.trust-badge {
  text-align: center;
  padding: 1.5rem;
}

.trust-icon {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.trust-title {
  font-weight: 600;
  font-size: var(--text-lg);
  margin-bottom: 0.5rem;
  color: var(--color-black);
}

.trust-text {
  font-size: var(--text-sm);
  color: var(--color-gray-600);
}

@media (max-width: 640px) {
  .trust-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }
}
</style>

{% schema %}
{
  "name": "Trust Badges",
  "settings": [
    {
      "type": "header",
      "content": "Badge 1"
    },
    {
      "type": "text",
      "id": "badge_1_title",
      "label": "Title",
      "default": "Fast Shipping"
    },
    {
      "type": "text",
      "id": "badge_1_text",
      "label": "Text",
      "default": "2-5 day delivery"
    },
    {
      "type": "header",
      "content": "Badge 2"
    },
    {
      "type": "text",
      "id": "badge_2_title",
      "label": "Title",
      "default": "Easy Returns"
    },
    {
      "type": "text",
      "id": "badge_2_text",
      "label": "Text",
      "default": "30-day guarantee"
    },
    {
      "type": "header",
      "content": "Badge 3"
    },
    {
      "type": "text",
      "id": "badge_3_title",
      "label": "Title",
      "default": "24/7 Support"
    },
    {
      "type": "text",
      "id": "badge_3_text",
      "label": "Text",
      "default": "Always here to help"
    },
    {
      "type": "header",
      "content": "Badge 4"
    },
    {
      "type": "text",
      "id": "badge_4_title",
      "label": "Title",
      "default": "Secure Payment"
    },
    {
      "type": "text",
      "id": "badge_4_text",
      "label": "Text",
      "default": "SSL encrypted"
    }
  ],
  "presets": [
    {
      "name": "Trust Badges"
    }
  ]
}
{% endschema %}
